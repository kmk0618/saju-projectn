# 부부궁합 생성지침 v4 — 원본 MD 고정 + 심층 내용 확장

## 제품 기준
- 기준 편집본: `부부궁합_최종본.md`
- 3 CHAPTER · 50 SECTION의 제목, 순서, 기본 편집 장치를 고정한다.
- PDF 디자인은 기존 부부궁합 원본 스타일을 고정한다. 내용 보강을 이유로 CSS·표·목차·폰트·여백을 임의 변경하지 않는다.

## 생성 순서
1. A/B 각각 `calcSaju()`로 만세력 계산
2. Person Meaning Map / Person Narrative 생성
3. Couple Cross Engine으로 일간·일지·오행·용신·합충형파해·원진·대운·세운 교차 계산
4. Couple Narrative / Relationship Loop / Love Translation 생성
5. Concept Registry로 50 SECTION 고유 역할 확정
6. SECTION Planner → 고객용 본문 생성
7. 깊이·근거·중복·편집장치 검수
8. 원본 디자인 렌더러로 PDF 생성

## 절대 원칙
- 원본 MD에 없는 반복 박스·판정표·템플릿 장치를 새로 만들지 않는다.
- 금지 예: `관계 점검 질문`, `이 SECTION의 판단 기준`, `유지/조정/경고`, `실제 생활에서 더 깊게 보면`, `관계 구조를 생활로 번역하면`.
- 페이지 수를 늘릴 때는 새 박스를 복제하지 않고 각 SECTION 고유의 새 해석을 추가한다.
- 이미 앞 SECTION에서 설명한 사주 원리는 뒤에서 재설명하지 않는다. 필요한 경우 1문장 참조만 허용한다.
- 각 SECTION은 Concept Registry의 `ownedConcepts`에 집중하고 `doNotRepeat`는 자세히 재설명하지 않는다.
- 생활 장면, A/B 관점, 의사결정 기준, 복구 행동, 실제 대화는 해당 SECTION 주제에 맞는 새 정보여야 한다.
- 동일한 문장뿐 아니라 의미상 같은 결론·조언·스크립트·생활장면의 반복도 차단한다.
- 고객 본문에 지침·프롬프트·엔진·calc·subset·구조 확인·참고 방법 같은 제작자 언어를 노출하지 않는다.

## 편집 디자인 고정
- 기존 부부궁합의 검정 표 헤더 + 가로 구분선 + 연한 교차행 배경 유지
- SECTION 부제는 원본처럼 제목 아래 일반 텍스트로 표현
- 원본에 있는 표·인용문·목록만 사용
- generic editorial supplement를 부부궁합에는 자동 추가하지 않는다.
- 목차 한글 단어 중간 줄바꿈 방지

## 분량 원칙
- 목표는 페이지 숫자 자체가 아니라 고유 정보 밀도다.
- 원본 디자인 기준 약 90페이지 전후를 목표로 하되, 중복 없는 실제 신규 정보가 충분할 때만 늘린다.
- SECTION 15·35·50은 앞 문장을 복사하는 요약이 아니라 판단 기준·실행 기준으로 재구성한다.

## 최종 검사
- 50 SECTION 존재
- 내부 제작 문구 0건
- 임의 반복 박스 0건
- 원시 검산표 0건
- 고유 생활 장면/스크립트/복구 행동 의미 중복 검사 통과
- PDF 직전 전체 감사 실패 시 생성 완료 처리 금지
